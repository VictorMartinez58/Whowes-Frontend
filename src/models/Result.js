class Result {
    constructor(title, user_id, totalPrice, users, products){
        this.title = title;
        this.user_id = user_id;
        this.totalPrice = totalPrice;
        this.users = users;
        this.products = products;
    }
}

export default Result;